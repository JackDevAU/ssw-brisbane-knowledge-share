---
import { SearchDialog } from './SearchDialog';
import type { Session } from '../types/session';

interface Props {
  sessions?: Session[];
}

const { sessions = [] } = Astro.props;
---

<header>
  <div class="header-content">
    <h1>🎱 SSW Brisbane Knowledge Share</h1>
    <p class="subtitle">Lunchtime learning and sharing</p>
    <div class="search-container">
      <SearchDialog client:load sessions={sessions} />
    </div>
  </div>
</header>

<style>
  header {
    background: var(--primary-color);
    color: white;
    padding: 2rem 0;
    margin-bottom: 2rem;
    position: relative;
  }

  .header-content {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 1rem;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
  }

  h1 {
    font-size: 2.5rem;
    margin: 0;
  }

  .subtitle {
    margin-bottom: 1rem;
  }

  .search-container {
    width: 100%;
    max-width: 24rem;
    margin: 0 auto;
  }
</style>